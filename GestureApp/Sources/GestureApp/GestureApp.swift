import SwiftUI

// Stub entry point — fully implemented in Task 14.
@main
struct GestureApp: App {
    var body: some Scene {
        MenuBarExtra("Gesture") {
            Text("Stub")
        }
    }
}
